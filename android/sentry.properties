# Sentry 프로덕션 설정
# https://sentry.io 에서 프로젝트 생성 후 아래 값 채우기

defaults.url=https://sentry.io/
defaults.org=YOUR_ORG_SLUG
defaults.project=near-price-app

# auth.token은 Sentry Settings > Auth Tokens에서 발급
# 이 파일은 git에 커밋하지 말것 (.gitignore에 추가 권장)
auth.token=YOUR_AUTH_TOKEN
